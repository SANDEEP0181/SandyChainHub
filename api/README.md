# Goalkeeper backend

The api directory contains server-side components and must not be deployed as static GitHub Pages code.

Set TELEGRAM_BOT_TOKEN as a backend environment secret. Never commit bot tokens, .env files, or private keys.