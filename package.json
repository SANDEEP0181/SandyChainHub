{
  "name": "sandychain-hub",
  "version": "0.1.0",
  "private": true,
  "description": "Testnet-only builder registry and community proposal hub.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sysfi": "hardhat run scripts/deploy-sysfi.js --network sysfi"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.0.0",
    "hardhat": "^2.22.0"
  }
}
