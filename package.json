{
  "name": "sandychain-hub",
  "version": "0.1.0",
  "private": true,
  "description": "Testnet-only builder registry and community proposal hub.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sysfi": "hardhat clean && hardhat compile && hardhat run scripts/deploy-sysfi.js --network sysfi",
    "deploy:sandymine": "hardhat clean && hardhat compile && hardhat run scripts/deploy-sandymine.js --network sysfi"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^7.0.0",
    "dotenv": "^17.2.2",
    "hardhat": "^2.22.0"
  }
}
